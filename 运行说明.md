# 混合 PDF 转 DOCX 转换器 - 使用说明

## 文件结构

现在的文件结构（已更新）：
```
FreeP2W/
├── hybrid_converter.py          # 主程序
├── demo.yaml                     # UniMERNet 配置文件
├── unimernet/                    # UniMERNet 模块
│   ├── __init__.py
│   ├── common/
│   ├── configs/
│   ├── datasets/
│   ├── models/
│   ├── processors/
│   ├── runners/
│   └── tasks/
├── weights/                      # YOLO 模型权重
│   └── doclayout_yolo_docstructbench_imgsz1024.pt
└── models/                       # UniMERNet 模型权重
    └── unimernet_small/
        └── unimernet_small.pth
```

## 运行主程序

### 1. 基本用法

```bash
python hybrid_converter.py --pdf <输入PDF文件> --output <输出DOCX文件>
```

### 2. 示例

```bash
# 转换 PDF 为 DOCX
python hybrid_converter.py --pdf test_files/2503.20314v2.pdf --output result.docx

# 使用默认输出名称（会自动命名为 input_hybrid.docx）
python hybrid_converter.py --pdf test_files/2503.20314v2.pdf
```

### 3. 高级选项

```bash
# 调整 YOLO 置信度阈值
python hybrid_converter.py --pdf input.pdf --output output.docx --yolo-conf 0.3

# 调整 YOLO 图像尺寸
python hybrid_converter.py --pdf input.pdf --output output.docx --yolo-imgsz 1536

# 禁用 YOLO（仅使用 pdf2docx）
python hybrid_converter.py --pdf input.pdf --output output.docx --no-yolo
```

## 功能特性

### ✅ 已实现的功能

1. **图像检测与提取**
   - 使用 DocLayout-YOLO 检测论文中的图像区域
   - 高精度图像提取，保留原始质量

2. **公式识别与转换**
   - 使用 UniMERNet 识别数学公式
   - 自动转换为 Word 可编辑的 MathML 格式
   - 保留公式编号（如 (1), (2)）

3. **内联数学符号处理**
   - 自动识别文本中的数学符号（如 θ, α, β）
   - 识别完整的数学表达式（如 `u(xt, ctxt, t; θ)`）
   - 自动处理下标（如 x₀, x₁, xₜ）
   - 支持分布表达式（如 `x₀ ∼ N(0, I)`）
   - 支持集合表达式（如 `t ∈ [0, 1]`）

4. **表格提取**
   - 使用 pdf2docx 提取表格
   - 保持表格格式与宽度

5. **文本提取**
   - 保留文本格式与布局
   - 智能过滤图像区域的重复文本

## 转换流程

1. **YOLO 检测**：检测所有页面的图像和公式区域
2. **公式识别**：使用 UniMERNet 识别公式并转换为 LaTeX
3. **PDF 转换**：使用 pdf2docx 提取文本、表格等内容
4. **公式插入**：将识别的公式以 MathML 格式插入到 DOCX
5. **内联数学处理**：识别并转换文本中的数学符号和表达式

## 输出质量

### 优点
- ✅ 图像清晰，分辨率高
- ✅ 公式可编辑（MathML 格式）
- ✅ 表格结构保持良好
- ✅ 文本格式基本保留
- ✅ 内联数学符号自动识别

### 注意事项
- ⚠️ 复杂布局可能需要手动调整
- ⚠️ 部分特殊字体可能无法完美保留
- ⚠️ 公式编号位置可能需要微调

## 故障排除

### 错误：找不到 demo.yaml
```
FileNotFoundError: Config file not found: E:\code\FreeP2W\demo.yaml
```
**解决方法**：确保 `demo.yaml` 文件在项目根目录

### 错误：找不到 unimernet 模块
```
ImportError: No module named 'unimernet'
```
**解决方法**：确保 `unimernet` 文件夹在项目根目录

### 错误：找不到模型权重
```
FileNotFoundError: [Errno 2] No such file or directory: './models/unimernet_small/unimernet_small.pth'
```
**解决方法**：确保模型权重文件存在于 `models/unimernet_small/` 目录

## 性能说明

- **GPU 支持**：自动检测并使用 CUDA（如果可用）
- **转换速度**：取决于 PDF 页数和公式数量
- **内存占用**：建议至少 8GB RAM，GPU 显存至少 4GB

## 下一步增强（可选）

1. **公式后处理**：修复 PDF 显示公式中的下标问题
2. **布局优化**：改进复杂布局的处理
3. **字体映射**：更好地保留特殊字体
4. **批量处理**：支持批量转换多个 PDF 文件

---

**最后更新**：2024-10-23
**测试状态**：✅ UniMERNet 加载成功，可以正常运行
