# 后处理格式化方案 - 使用指南

## 📋 方案说明

采用**两步走**策略：
1. **步骤1**：使用 `hybrid_converter.py` 进行 PDF 转换（保留公式识别优势）
2. **步骤2**：使用 `post_format_english_body.py` 统一英文正文格式

这样既保留了公式识别的优势，又实现了格式统一。

---

## 🚀 快速开始

### 方法1：一键完整流程（推荐）

```bash
python convert_and_format.py test_files/2503.20314v2.pdf result_final.docx
```

这会自动完成：
- ✅ PDF 转 DOCX（保留公式）
- ✅ 统一英文正文字体为 Times New Roman 10pt
- ✅ 统一英文正文为两端对齐
- ✅ 保留标题、图表标题、公式的原格式

### 方法2：分步执行（用于调试）

**步骤1：转换 PDF**
```bash
python -c "from hybrid_converter import HybridConverter; c = HybridConverter(); c.convert('test_files/2503.20314v2.pdf', 'result.docx')"
```

**步骤2：格式化正文**
```bash
python post_format_english_body.py
```

---

## 📖 详细说明

### 后处理格式化脚本功能

`post_format_english_body.py` 会：

**✅ 格式化正文：**
- 统一字体：Times New Roman
- 统一大小：10pt
- 统一对齐：两端对齐（JUSTIFY）

**✅ 自动识别并保留：**
- 标题（大字体、粗体、全大写短文本）
- 图表标题（Figure X, Table X 开头）
- 目录（TOC）
- 数学公式（OMML 元素）
- 太短的段落（< 20 字符）

**✅ 智能处理：**
- 只格式化包含英文的段落
- 保留每个 run 的原始结构（不合并 runs）
- 每个 run 单独设置字体和大小

---

## ⚙️ 自定义配置

### 修改字体和大小

编辑 `post_format_english_body.py` 底部：

```python
format_english_body_text(
    input_path="result.docx",
    output_path="result_formatted.docx",
    target_font='Times New Roman',  # 修改这里：字体名称
    target_size=10.0,                # 修改这里：字体大小（pt）
    target_alignment=WD_ALIGN_PARAGRAPH.JUSTIFY  # 对齐方式
)
```

**常用字体：**
- `'Times New Roman'` - 经典学术字体
- `'Arial'` - 现代无衬线字体
- `'Calibri'` - Office 默认字体
- `'Georgia'` - 易读衬线字体

**常用字体大小：**
- `10.0` - 标准正文（推荐）
- `11.0` - 较大正文
- `12.0` - 大号正文

### 修改对齐方式

```python
from docx.enum.text import WD_ALIGN_PARAGRAPH

# 可选对齐方式：
target_alignment = WD_ALIGN_PARAGRAPH.LEFT      # 左对齐
target_alignment = WD_ALIGN_PARAGRAPH.CENTER    # 居中
target_alignment = WD_ALIGN_PARAGRAPH.RIGHT     # 右对齐
target_alignment = WD_ALIGN_PARAGRAPH.JUSTIFY   # 两端对齐（默认）
```

---

## 📊 格式化效果示例

### 处理前：
```
段落 1: "Introduction to machine learning..."
  Run 1: "Introduction" (None font, None size)
  Run 2: "to" (None font, 11pt)
  Run 3: "machine learning" (Arial, 10pt)
  对齐: LEFT

段落 2: "The algorithm uses x0 ∼N(0, I)..."
  Run 1: "The algorithm" (None font, 10pt)
  Run 2: "uses" (None font, 12pt)
  Run 3: [OMML Math]
  对齐: None
```

### 处理后：
```
段落 1: "Introduction to machine learning..."
  Run 1: "Introduction" (Times New Roman, 10pt)
  Run 2: "to" (Times New Roman, 10pt)
  Run 3: "machine learning" (Times New Roman, 10pt)
  对齐: JUSTIFY

段落 2: "The algorithm uses x0 ∼N(0, I)..."
  Run 1: "The algorithm" (Times New Roman, 10pt)
  Run 2: "uses" (Times New Roman, 10pt)
  Run 3: [OMML Math] ← 保持不变
  对齐: JUSTIFY
```

---

## 🔍 格式化统计示例

运行后会显示详细统计：

```
================================================================================
[完成] 格式化统计:
================================================================================
总段落数:          52
正文段落数:        32
已格式化段落:      32
已格式化 runs:     158

跳过的段落:
  - 标题:          10
  - 图表标题:      5
  - 目录:          3
  - 包含公式:      13
  - 太短:          4
================================================================================
```

这说明：
- ✅ 识别了 32 个正文段落并格式化
- ✅ 格式化了 158 个文本 runs
- ✅ 正确跳过了标题、图表、公式等特殊内容

---

## 🎯 判断逻辑说明

### 如何识别"正文"？

脚本使用以下逻辑：

**1. 标题识别：**
```python
- 字体 >= 14pt
- 全大写且 < 100 字符
- 加粗且 < 100 字符
→ 跳过，保持原格式
```

**2. 图表标题识别：**
```python
- 以 "Figure X", "Table X", "Fig. X" 开头
→ 跳过，保持原格式
```

**3. 目录识别：**
```python
- 检测 "Contents", "Table of Contents" 关键词
- 从目录开始到第一个长段落
→ 跳过，保持原格式
```

**4. 公式识别：**
```python
- 段落包含 <m:oMath> 元素
→ 跳过，保持原格式
```

**5. 正文识别：**
```python
- 不是标题
- 不是图表标题
- 不在目录中
- 长度 >= 20 字符
- 包含英文字母
→ 格式化为 Times New Roman 10pt 两端对齐
```

---

## 🛠️ 高级用法

### 仅格式化特定页面

编辑 `post_format_english_body.py`，在 `is_body_paragraph` 中添加页面范围判断。

### 排除特定段落

在 `is_body_paragraph` 中添加自定义规则：

```python
def is_body_paragraph(para, para_idx, toc_range):
    text = para.text.strip()

    # 排除包含特定关键词的段落
    if any(keyword in text.lower() for keyword in ['acknowledgments', 'references', 'appendix']):
        return False

    # 原有逻辑...
```

### 只格式化特定字体大小范围

在格式化循环中添加条件：

```python
for run in para.runs:
    if contains_english(run.text):
        # 只格式化 9-12pt 的文本
        if run.font.size and 9 <= run.font.size.pt <= 12:
            run.font.name = target_font
            run.font.size = Pt(target_size)
```

---

## ✅ 优势总结

**相比直接修改 hybrid_converter：**

| 方面 | 后处理方案 | 直接修改方案 |
|------|-----------|------------|
| **实现难度** | ✅ 简单，独立脚本 | ❌ 复杂，需深入理解转换器 |
| **灵活性** | ✅ 随时调整参数 | ❌ 需要重新转换 |
| **调试** | ✅ 快速迭代测试 | ❌ 每次测试需完整转换 |
| **公式保留** | ✅ 完全不影响 | ⚠️ 可能影响公式 |
| **可维护性** | ✅ 逻辑清晰 | ❌ 耦合度高 |

**相比手动调整：**
- ✅ 自动化，节省时间
- ✅ 一致性，避免遗漏
- ✅ 可重复，批量处理
- ✅ 智能识别，避免误格式化

---

## 📁 文件说明

| 文件 | 说明 |
|------|------|
| `hybrid_converter.py` | PDF 转 DOCX（保留公式） |
| `post_format_english_body.py` | 后处理格式化脚本 |
| `convert_and_format.py` | 一键完整流程脚本 |
| `result.docx` | 初步转换结果 |
| `result_formatted.docx` | 格式化后结果 |
| `result_final.docx` | 完整流程最终结果 |

---

## 🐛 常见问题

### Q: 某些段落没有被格式化？
A: 检查是否被识别为标题、图表标题或包含公式。查看统计中的"跳过的段落"数量。

### Q: 想格式化中文段落？
A: 修改 `is_body_paragraph` 中的 `contains_english` 判断，或直接移除该条件。

### Q: 格式化后公式显示异常？
A: 脚本会自动跳过包含公式的段落。如果仍有问题，检查公式段落是否被误识别为正文。

### Q: 想保留原有字体？
A: 在格式化循环中添加条件，只修改对齐方式：
```python
para.alignment = target_alignment
# 注释掉字体修改部分
```

---

## 🎉 总结

后处理格式化方案的核心优势：

1. **保留公式识别优势** - 不改变 hybrid_converter 的核心功能
2. **灵活可配置** - 随时调整字体、大小、对齐方式
3. **智能识别** - 自动区分正文、标题、公式
4. **快速迭代** - 不满意可以快速重新格式化
5. **易于维护** - 逻辑清晰，代码独立

现在可以开始使用了！运行：

```bash
python convert_and_format.py test_files/2503.20314v2.pdf result_final.docx
```

或者如果已经有 `result.docx`，直接运行：

```bash
python post_format_english_body.py
```

享受自动化格式统一的便利！🚀
